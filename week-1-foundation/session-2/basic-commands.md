Commands used to initialise a repo: git init

Good commit messages when commiting:
 git commit -m "Initial commit"
 git commit -m "Fix spelling errors in REAME"

Command to check all commits:
 git log 
 git log --oneline

Most personally commands in Git and CLI:
 Git cammands-
 git status
 git add
 git commit -m "message"
 git log --oneline

 CLI commands-
 ls 
 cd
 pwd
 mkdir
 nano
 touch
 clear
 
